services:
  - type: web
    name: pentavault
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PENTAVAULT_FRONTEND_MODE
        value: react
      - key: PENTAVAULT_GEMINI_API_KEYS
        sync: false
